export EDITOR='subl'

# boxen setup if it exists
[ -f /opt/boxen/env.sh ] && source /opt/boxen/env.sh
# [ -f $BOXEN_NVM_DIR/nvm.sh ] && 
#   source $BOXEN_NVM_DIR/nvm.sh  && 
#   nvm use v0.8 > /dev/null 2>&1